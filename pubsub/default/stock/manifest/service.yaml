apiVersion: v1
kind: Service
metadata:
  labels:
    app: stock
  name: stock
spec:
  ports:
  - name: "5000"
    port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: stock
  type: ClusterIP
status:
  loadBalancer: {}